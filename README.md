# Retail Intelligence Platform

## Overview

Retail Intelligence Platform is an end-to-end analytics project built to transform raw retail datasets into clean, business-ready insights. The project uses two Kaggle retail datasets and is being developed as a professional portfolio project covering data cleaning, SQL analytics, dashboarding, and forecasting.

The goal is to answer practical retail business questions:

- Which products, stores, and customer segments drive revenue?
- How do sales patterns change over time?
- Which features help explain or forecast retail performance?
- How can cleaned data support dashboards and decision-making?

## Current Status

Current Phase: Staging Models

This repository is moving from raw PostgreSQL loading into typed staging models for business-ready analysis.

Completed:

- Created professional repository structure.
- Added repeatable data cleaning script.
- Cleaned missing, duplicate, and invalid records.
- Generated local processed datasets.
- Added documentation placeholders for future project work.

In progress:

- PostgreSQL schema architecture.
- Retail analytics star schema design.
- Python ETL loading into PostgreSQL.
- PostgreSQL staging models.
- Exploratory analysis notebooks.
- SQL KPI queries and analytical views.
- Dashboard design and screenshots.
- Forecasting model development.

## Tech Stack

- Python
- pandas
- NumPy
- Jupyter Notebook
- SQL
- Power BI
- scikit-learn
- Git and GitHub

## Repository Structure

```text
retail-intelligence-platform/
|-- data/
|   |-- raw/
|   |-- processed/
|   `-- external/
|-- notebooks/
|-- sql/
|-- src/
|-- dashboard/
|-- docs/
|-- screenshots/
|-- architecture/
|-- README.md
|-- requirements.txt
`-- .gitignore
```

## Data Policy

Large datasets are not committed to GitHub.

The `data/` directory is ignored by Git to keep the repository lightweight and reproducible. Raw Kaggle downloads and cleaned outputs should stay on the local machine.

Expected local layout:

```text
data/
|-- raw/          # Original Kaggle files
|-- processed/    # Cleaned outputs generated by scripts
`-- external/     # Optional external reference data
```

## Data Cleaning

The first transformation step is implemented in:

```text
src/transform.py
```

It currently:

- Removes duplicate rows.
- Handles missing and incomplete records.
- Removes invalid business records such as cancelled invoices and negative sales.
- Standardizes key date and text fields.
- Creates cleaned local outputs in `data/processed/`.
- Generates a local data quality report.

Run the cleaning script:

```bash
python src/transform.py
```

## Loading Cleaned Data Into PostgreSQL

Cleaned CSV files can be loaded into the PostgreSQL `raw` schema with:

```text
src/load_to_postgres.py
```

The loader:

- Reads all CSV files from `data/processed/`.
- Derives PostgreSQL table names from CSV filenames.
- Loads each file into the `raw` schema.
- Uses `if_exists="replace"` for development refreshes.
- Reads database credentials from environment variables or a local `.env` file.

Create a local `.env` file from `.env.example`:

```text
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=retail_warehouse
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
```

Run the loader:

```bash
pip install -r requirements.txt
python src/load_to_postgres.py
```

## Architecture

Planned data flow:

```text
CSV Files
      |
      v
raw schema
      |
      v
staging schema
      |
      v
warehouse schema
      |
      v
marts schema
      |
      +--> Power BI
      +--> Machine Learning
      `--> FastAPI
```

Architecture documentation is available in:

- `docs/warehouse_architecture.md`
- `docs/star_schema.md`

## Roadmap

- Complete exploratory data analysis notebooks.
- Build SQL tables, views, and KPI queries.
- Create a Power BI dashboard.
- Add dashboard screenshots.
- Train and evaluate baseline forecasting models.
- Document business insights and recommendations.
- Add final architecture diagram.

## How to Reproduce

1. Clone the repository.
2. Create a Python virtual environment.
3. Install dependencies.
4. Download the Kaggle datasets locally into `data/raw/`.
5. Run the cleaning script.

```bash
pip install -r requirements.txt
python src/transform.py
```

## Commit Plan

Use small, clear commits:

1. `chore: initialize repository structure`
2. `docs: add professional project README`
3. `chore: add gitignore and dependency file`
4. `docs: add data cleaning documentation`
5. `feat: add data cleaning pipeline`
6. `docs: add dashboard screenshots and project walkthrough`

## Author

Built as a data analytics portfolio project focused on retail intelligence, business reporting, and forecasting.
